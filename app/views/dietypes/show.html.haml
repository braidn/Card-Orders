%label{:class => "label"}= t("activerecord.attributes.dietype.dieDescription", :default => t("activerecord.labels.dieDescription", :default => "Diedescription")) + ":"
%p= @dietype.dieDescription

.form-actions
  = link_to "Back", dietypes_path, :class => 'btn'
  = link_to "Edit", edit_dietype_path(@dietype), :class => 'btn'
  = link_to "Delete", dietype_path(@dietype), :method => "delete", :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}", :class => 'btn btn-danger'
