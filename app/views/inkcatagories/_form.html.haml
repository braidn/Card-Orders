= form_for(@inkcatagory) do |f|
  - if @inkcatagory.errors.any?
    #error_explanation
      %h2
        = pluralize(@inkcatagory.errors.count, "error")
        prohibited this inkcatagory from being saved:
      %ul
        - @inkcatagory.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :description
    %br/
    = f.text_area :description
  .actions
    = f.submit
