%h1 Companys
%table{:class => "table table-striped"}
  %thead
    %tr
      %th ID
      %th
        = t("activerecord.attributes.company.address1", :default => t("activerecord.labels.address1", :default => "Address1"))
      %th Created at
      %th Actions
  %tbody
    - @companies.each do |company|
      %tr
        %td= company.id
        %td= link_to company.address1, company_path(company)
        %td= company.created_at
        %td
          = link_to "Show", company_path(company), :class => 'btn btn-mini'
          = link_to "Edit", edit_company_path(company), :class => 'btn btn-mini'
          = link_to "Destroy", company_path(company), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}", :class => 'btn btn-mini btn-danger' 

= link_to "New", new_company_path, :class => 'btn btn-primary'
