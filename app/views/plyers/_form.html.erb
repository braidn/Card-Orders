<%= form_for(@plyer) do |f| %>
  <% if @plyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plyer.errors.count, "error") %> prohibited this plyer from being saved:</h2>

      <ul>
      <% @plyer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
