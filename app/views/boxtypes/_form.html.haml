%fieldset
  %legend
    = controller.action_name.capitalize
    Boxtype
  .control-group
    = f.label :boxid, 'Box Id', :class => 'control-label'
    .controls
      = f.number_field :boxid, :class => 'number_field'
  .control-group
    = f.label :boxtype, 'Box Type',  :class => 'control-label'
    .controls
      = f.text_field :boxtype, :class => 'text_field'
  .form-actions
    = f.submit "Create Box Type", :class => 'btn btn-primary'
    = link_to "Cancel", boxtypes_path, :class => 'btn'
