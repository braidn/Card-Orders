%h1 Listing foiltypes
%table
  %tr
    %th Type
    %th
    %th
    %th
  - @foiltypes.each do |foiltype|
    %tr
      %td= foiltype.type
      %td= link_to 'Show', foiltype
      %td= link_to 'Edit', edit_foiltype_path(foiltype)
      %td= link_to 'Destroy', foiltype, confirm: 'Are you sure?', method: :delete
%br/
= link_to 'New Foiltype', new_foiltype_path
