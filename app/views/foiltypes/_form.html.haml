%fieldset
  %legend
    = controller.action_name.capitalize
    /Foiltype
  .control-group
    = f.label :type, :class => 'control-label'
    .controls
      = f.text_field :type, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to "Cancel", foiltypes_path, :class => 'btn'
