<h1>Listing sales_reps</h1>

<table>
  <tr>
    <th>Orderid</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sales_reps.each do |sales_rep| %>
  <tr>
    <td><%= sales_rep.orderId %></td>
    <td><%= sales_rep.name %></td>
    <td><%= link_to 'Show', sales_rep %></td>
    <td><%= link_to 'Edit', edit_sales_rep_path(sales_rep) %></td>
    <td><%= link_to 'Destroy', sales_rep, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sales rep', new_sales_rep_path %>
