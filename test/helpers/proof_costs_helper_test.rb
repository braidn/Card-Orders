require "minitest_helper"

class ProofCostsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= ProofCostsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
