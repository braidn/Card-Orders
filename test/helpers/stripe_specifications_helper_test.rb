require "minitest_helper"

class StripeSpecificationsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= StripeSpecificationsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
