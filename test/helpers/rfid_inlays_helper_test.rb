require "minitest_helper"

class RfidInlaysHelperTest < MiniTest::Rails::Helper
  before do
    @helper= RfidInlaysHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
