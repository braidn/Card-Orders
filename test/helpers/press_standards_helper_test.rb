require "minitest_helper"

class PressStandardsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= PressStandardsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
