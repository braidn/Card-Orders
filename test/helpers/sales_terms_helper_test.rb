require "minitest_helper"

class SalesTermsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= SalesTermsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
