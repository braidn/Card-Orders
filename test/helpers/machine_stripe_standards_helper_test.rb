require "minitest_helper"

class MachineStripeStandardsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= MachineStripeStandardsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
