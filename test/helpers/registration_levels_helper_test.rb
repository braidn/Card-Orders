require "minitest_helper"

class RegistrationLevelsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= RegistrationLevelsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
