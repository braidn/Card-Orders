require "minitest_helper"

class MachineIdStandardsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= MachineIdStandardsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
