require "minitest_helper"

class ProductFormatsHelperTest < MiniTest::Rails::Helper
  before do
    @helper= ProductFormatsHelper.new
  end

  it "must be a real test" do
    flunk "Need real tests"
  end
end
